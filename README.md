# hardhat-web3.js 项目后端模板

```shell
yarn hardhat deploy --network localnetwork
yarn hardhat deploy --network Goerli
yarn hardhat test --network localnetwork
yarn hardhat test --network Goerli
yarn solhint 'contracts/*.sol'
yarn solhint 'contracts'/*.sol --fix
yarn prettier --write .
```
